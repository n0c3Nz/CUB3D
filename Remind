Cosas a tener en cuenta:

map->file:
	· No guarda las líneas que solo contienen un salto de línea.
map->map->c && f:
        · Añadir verificación de que los numeros >= 0 <= 255

        · Asegurar mallocs
        · Encontrar Segfault

